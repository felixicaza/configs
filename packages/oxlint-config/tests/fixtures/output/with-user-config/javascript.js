// Code generated by AI for testing purposes

const logs = []

function out(value) {
  logs.push(String(value))
}

const name = 'Ada'
const powerBase = 2
const powerExp = 3
const powerResult = powerBase ** powerExp
const label = `Power result: ${  powerResult}`

const profile = {
  name,
  'role': 'admin',
  points: powerResult
}

const { role: currentRole } = profile

if (currentRole === 'admin')
  {out(label)}

const escaped = 'hello\\,world'
out(escaped)

const hasRole = !!currentRole
if (hasRole) {
  out('Role ok')
}

if (
  profile.points > 0
)
  {out('Points available')}

if (logs.length > 0) {
  out('done')
}
